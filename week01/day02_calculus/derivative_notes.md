# Day 2 · 导数与反向传播

> Week 1 · Day 2 —— 微积分：只围绕优化和反向传播
> 手推过程可以直接写在纸上拍照，这份文件用来记**结论和卡住的地方**。

---

## 核心一句话

> **反向传播 = 链式法则 + 从后往前传。**

每个节点做的事只有一件：

```
        局部导数  ×  上游梯度
        （只看自己）  （从后面传回来的）
```

**关键**：局部导数只跟自己有关，不用管前面后面长什么样。
所以每个算子只需要知道"我的输入变一点，我的输出变多少"，整条链就自动串起来了。

---

## 练习 1：y = (w·x + b)²

**设** u = w·x + b，则 y = u²

```
先求 dy/du = ?
再对每个变量用链式法则：dy/d? = dy/du × du/d?
```

**我的推导**（写在纸上，或者写这里）：

```
dy/dw =

dy/db =

dy/dx =
```

**验证**：填进 `grad_check.py` 的 `grad1()`，跑一下。

---

## 练习 2：z = sigmoid(w2 · relu(w1·x + b1) + b2)

**拆成四步**：

```
h = w1·x + b1          一层的线性部分
a = relu(h)            一层的激活
u = w2·a + b2          二层的线性部分
z = sigmoid(u)         二层的激活
```

**从后往前推**：

```
dz/du  =

dz/da =              dz/dw2 =
                     dz/db2 =

dz/dh =              ← 这里要乘上 relu 的导数

dz/dw1 =             dz/db1 =            dz/dx =
```

**验证**：填进 `grad_check.py` 的 `grad2()`，有两个测试点——
一个 relu 通着，一个 relu 关着。**两组都要过。**

**卡住的地方记在这**：

```

```

---

## 完成标准（计划里的验收）

不查资料，用自己的话解释这三条：

### 1. gradient 是什么？

（写你自己的话）


### 2. chain rule 为什么允许从 loss 一路传回参数？

（写你自己的话）


### 3. 为什么 relu 关掉时，上游梯度会变成 0？

（写你自己的话）


---

## 打卡

- [ ] 理论完成（derivative / partial derivative / gradient / chain rule）
- [ ] 代码完成（`grad_check.py` 两个练习都填了）
- [ ] 实验完成（三组测试全部通过）
- [ ] 当日笔记完成
